// This component is used to render the AddNotaryModal component
// Importing the AddNotaryModal component
import AddNotaryModal from "./AddNotaryModal";

// Define the AddProduct component
const AddNotary = () => {
  return (
    <div className="flex justify-start">
      {/* Render the AddNotaryModal component */}
      <AddNotaryModal />
    </div>
  );
};

export default AddNotary;